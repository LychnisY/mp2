export interface Student {
  id: string;                 
  name: string;
  school: string;
  damageType: string;         
  birthday?: string;
  portraitUrl?: string;       
  schoolImageUrl?: string;    
}